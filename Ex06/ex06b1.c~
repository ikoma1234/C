#include <stdio.h>
 
int main()
{
 
  int i,num;
 
  int a = 1;
 
  printf("階乗を計算したい数を入力してください： ");
  scanf("%d", &num);
  if( num > 12 || num < 0)
    printf("計算できません\n");
  else
    {
     for(i=1; i<=num; ++i) {
        a = a * i;
     }
    
 
     printf("%d\n", a);
    }
 
  return 0;
}
